{erl_opts, [
    debug_info,
    warnings_as_errors
]}.

{deps, [
    {riakc,
        {git, "git://github.com/basho/riak-erlang-client.git", {branch, "develop"}}}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [reliable]}
]}.

